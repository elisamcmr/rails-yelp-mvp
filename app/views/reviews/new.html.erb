<div class="container mt-5">
  <div class="card p-4 shadow-lg border-0">
    <h1 class="mb-4 text-primary fw-bold">
      Add a New Review
    </h1>

    <%= render 'form', review: @review %>
  </div>

  <%# <div class="mt-3">
    <%= link_to "Back to restaurant", restaurant_path, class: "btn btn-secondary" %>
  </div>
</div>
